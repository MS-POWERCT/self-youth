## 项目提醒

1. 使用备份前请重新创建新的 `.env`（`APP_KEY`）

```bash
php artisan key:generate
```

2. 创建新的 `storage/oauth-private.key` 和 `storage/oauth-public.key`

```bash
php artisan passport:keys
```

如果已存在则执行（覆盖）：

```bash
php artisan passport:keys --force
```

3. 注意 `admin_user` 的 Google 验证码需要重新创建
    - 需要修改下面这个文件后再刷新：`DcatAuthGoogle2FaUserController.php`

4. 如果邮箱发送记得配置 SSL（直接使用 macOS 系统证书）

```bash
sudo mkdir -p /usr/local/etc/openssl@1.1/
sudo ln -sf /etc/ssl/cert.pem /usr/local/etc/openssl@1.1/cert.pem
```
