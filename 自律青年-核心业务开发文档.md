# 自律青年\-核心业务模块完整开发文档（最终开发版）

## 前置说明

本项目核心业务只有两大模块，全部自律、记录、成长数据，所有数据**100%云端存储、绑定用户账号、永久留存**。

剔除所有冗余功能，只保留核心，后期可无限扩展。

**核心两大模块**

1. **习惯打卡模块（日常作息类）** —— 布尔型：今日做 or 没做

2. **时长/计数记录模块（自律精进类）** —— 数值型：做了多久/多少次

所有数据**按用户隔离、按日期归档、支持日历回溯、支持统计曲线**

---

## 一、模块整体业务定义

### 1\. 习惯打卡模块（打卡型）

**适用场景：只区分「完成 / 未完成」的日常作息**

例：吃早餐、早刷牙、晚刷牙，读书打卡、冥想打卡、洗脸护肤、泡脚洗脚等

**特点：无数量、无时长，只有每日状态**

### 2\. 时长计数记录模块（数值型）

**适用场景：需要记录「时长 / 次数」的自律行为**

例：看书、锻炼身体、规律喝水、冥想

**特点：支持多次记录、累加、单日总量、历史回溯**

---

## 二、数据表设计（最终版，直接 migrate）

### 表1：user_habits 用户习惯配置表（核心配置）

作用：用户自己开启/关闭/排序/自定义的所有习惯列表

**区分 type=1 打卡型 / type=2 数值型**

```sql
CREATE TABLE `user_habits` (
  `id` int unsigned NOT NULL AUTO_INCREMENT,
  `user_id` int unsigned NOT NULL COMMENT '用户ID',
  `name` varchar(100) NOT NULL COMMENT '习惯名称',
  `type` tinyint NOT NULL COMMENT '1打卡型 2数值型',
  `icon` varchar(255) DEFAULT '' COMMENT '图标',
  `sort` int DEFAULT 0 COMMENT '排序权重',
  `is_show` tinyint DEFAULT 1 COMMENT '是否显示',
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户习惯配置表';
```

### 表2：habit_check_logs 习惯打卡记录表（对应 type=1）

只存每日打卡状态，一天一条

```sql
CREATE TABLE `habit_check_logs` (
  `id` int unsigned NOT NULL AUTO_INCREMENT,
  `user_id` int unsigned NOT NULL,
  `habit_id` int unsigned NOT NULL,
  `check_date` date NOT NULL COMMENT '打卡日期',
  `status` tinyint DEFAULT 1 COMMENT '1完成',
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  UNIQUE KEY `user_habit_date` (`user_id`,`habit_id`,`check_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='打卡型习惯日志';
```

### 表3：habit_value_logs 数值记录日志表（对应 type=2）

支持一天多条记录、自动累加，适合计时/计数

```sql
CREATE TABLE `habit_value_logs` (
  `id` int unsigned NOT NULL AUTO_INCREMENT,
  `user_id` int unsigned NOT NULL,
  `habit_id` int unsigned NOT NULL,
  `value` int NOT NULL DEFAULT 0 COMMENT '数值：分钟/次数',
  `unit` varchar(20) DEFAULT '' COMMENT '单位：分钟、次',
  `record_date` date NOT NULL COMMENT '记录日期',
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `user_date` (`user_id`,`record_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='数值型习惯记录日志';
```

---

## 三、两大模块完整业务逻辑

### 1、习惯打卡模块（type=1）逻辑

**核心规则：一个习惯，一天只能打卡一次**

- 用户每日对某个习惯「点击打卡」

- 当天已存在记录 → 可取消打卡（软删除/状态置0）

- 当天无记录 → 新增今日打卡记录

- 日历可查看每一天是否完成

- 支持统计：本周完成率、本月打卡天数、连续打卡天数

### 2、数值记录模块（type=2）逻辑

**核心规则：一天可多次记录，自动累加**

- 支持随时新增一条记录（比如看书 35 分钟）

- 同一天多条记录自动汇总单日总量

- 可删除单条记录

- 可查看每日明细

- 支持周/月曲线统计

---

## 四、完整接口清单（可直接开发）

### 第一部分：用户习惯配置接口（通用）

所有接口需要 Token 登录态

1. **获取我的习惯列表** GET /api/habit/list

2. **新增自定义习惯** POST /api/habit/add

3. **编辑习惯** POST /api/habit/edit

4. **删除习惯** POST /api/habit/del

5. **排序/显示隐藏** POST /api/habit/sort

### 第二部分：打卡型习惯接口（type=1）

1. **今日打卡/取消打卡** POST /api/habit/check/toggle

2. **获取某月日历打卡数据** GET /api/habit/check/calendar

3. **获取打卡统计（周/月）** GET /api/habit/check/stat

### 第三部分：数值型习惯接口（type=2）

1. **新增一条数值记录** POST /api/habit/value/add

2. **删除单条记录** POST /api/habit/value/del

3. **获取某日记录列表** GET /api/habit/value/day\-list

4. **获取数值统计曲线** GET /api/habit/value/chart

---

## 五、默认初始化习惯数据（用户首次登录自动生成）

新用户首次登录成功，自动初始化一套系统默认习惯，不用用户手动创建，体验极佳

### 默认打卡型（type=1）

- 早睡、早起、吃早餐、每日运动、喝水打卡、冥想打卡

### 默认数值型（type=2）

- 阅读学习（分钟）、运动时长（分钟）、冥想时长（分钟）、喝水次数（次）

---

## 六、整体开发顺序（不会乱、不返工）

**严格按这个顺序开发，最快落地**

1. 执行三张数据表迁移

2. 创建 Model \+ 对应 Resource

3. 写「新用户首次登录自动初始化默认习惯」逻辑

4. 开发习惯配置 CRUD 接口

5. 开发【打卡型】全套逻辑与接口

6. 开发【数值型】全套逻辑与接口

7. 开发日历数据、统计、曲线接口

8. Vue3 前端对接页面：习惯列表、打卡页、记录页、统计页

---

## 七、可后期扩展（现在不做，预留）

- 亲友绑定监督模块

- 每日文案/提示内容模块

- 连续打卡奖励、成就系统

- 数据年度复盘

---

## 八、文档最终总结

整个项目现在固定三大业务模块：

1. **账户模块（已完成）**：邮箱验证码自动注册登录、密码登录、Web3登录、个人资料

2. **核心习惯打卡模块（当前开发）**

3. **核心数值记录模块（当前开发）**

结构干净、完全解耦、可长期迭代、数据永久可回溯，完全对标成熟自律产品架构。

> （注：文档部分内容可能由 AI 生成）
